# دليل رفع الموقع على Netlify

لرفع هذا المشروع (Next.js مع Supabase) على استضافة Netlify، اتبع الخطوات التالية:

## 1. الملفات المطلوبة للرفع
إذا كنت ترفع يدويًا، يجب رفع المجلد كاملاً **باستثناء**:
- مجلد `node_modules` (يتم إنشاؤه تلقائياً أثناء البناء).
- مجلد `.next` (يتم إنشاؤه أثناء البناء).
- ملف `.env.local` (يجب إدخال المتغيرات في إعدادات Netlify وليس في الملف).

> [!IMPORTANT]
> الطريقة الأفضل هي ربط مستودع GitHub الخاص بك بـ Netlify ليقوم بالبناء التلقائي عند كل تحديث.

## 2. إعدادات البناء (Build Settings)
عند إعداد الموقع في Netlify، تأكد من القيم التالية:
- **Build Command:** `npm run build`
- **Publish Directory:** `.next`
- **Node Version:** تأكد من أنها 18 أو أحدث.

## 3. متغيرات البيئة (Environment Variables)
يجب إضافة المتغيرات التالية في Site Settings -> Environment Variables في لوحة تحكم Netlify:

| المتغير | الوصف |
| :--- | :--- |
| `NEXT_PUBLIC_SUPABASE_URL` | رابط مشروع Supabase الخاص بك |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | مفتاح Anon Key الخاص بمشروعك |

## 4. الملحقات الضرورية
Netlify سيتعرف تلقائيًا على أنه مشروع Next.js وسيقوم بتثبيت `@netlify/plugin-nextjs` لضمان عمل وظائف السيرفر (Server Components) والـ Middleware بشكل صحيح.

---
**بالتوفيق في مشروعك!** 🚀
